@model UpdateUserRoleVm

@{ 
// razor code here if yo need that
}

<div  >
    <hr />
    
    <div class="text-center">
        <h2 >Adding Role to a User</h2>
        <span>Chose the role and the user respectively and click add role</span>
    </div>
    <hr />
    <form asp-controller="Roles" asp-action="UpdateUserRole" method="post">
        
        <div class="d-flex p-2 justify-content-center">
            <br />
            <div>
                <h4>Roles</h4>
                @foreach (var role in @Model.Roles)
                {
        <div>
            <input type="radio" asp-for="Role" value="@role" />
            @role
        </div>}
            </div>
            @*<input class="btn btn-outline-primary" type="submit" value="Add Role"/>
        <input type="hidden" asp-for="Delete" value="false" id="delete"/>
        <input class="btn btn-outline-danger" type="submit" value="Delete Role" onclick="document.getElementById('delete').value = 'true'"/>*@
            <div style="padding-left: 350px">
                 
                <h4>Users</h4>
                <div>
                    @foreach (var user in @Model.Users)
                    {
        <div>
            <input type="radio" asp-for="@Model.UserEmail" value="@user.Email" />
            @user.Email
        </div>
                        <div>
                            @foreach (var role in user.Roles)
                            {
            <h5>
                @role.Name
            </h5>}
                        </div>}
                </div>
            </div>
        </div>
        
        <div style="text-align:center;" class="m-5">
            <input class="btn btn-outline-success" type="submit" value="Add Role" />
            <input type="hidden" asp-for="Delete" value="false" id="delete" />
            <input class="btn btn-outline-danger" type="submit" value="Delete Role" onclick="document.getElementById('delete').value = 'true'" />
        </div>
    </form>
</div>